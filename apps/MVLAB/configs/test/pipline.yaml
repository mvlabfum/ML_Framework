tname_step:
  forward:
    - input: [z, randn, [$zdim, 1, 1]]  # input z
    - seq0
    - Return: NET_LOSS
  
  seq0:
    input:
    output: NET_LOSS
    head:
      - BB.Self:
          fn: tname
          input: [z, y]
      - BB.Self:
          fn: tnameLOSS
          input: [[y], [t]]